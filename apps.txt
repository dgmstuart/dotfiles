Slack
Pop

Chrome
Spotify

iterm2 (remember to install shell integrations to get inline images)
asdf (install with git, not with brew)
nvm
Postgres.app
VScode

1Password

homebrew
oh-my-zsh
solargraph (gem)
tmuxinator (gem)
k (git submodule init; git submodule update)
instant-markdown-d (npm)

npm install -g typescript (Language server)

yarn (run `corepack enable`)

tpm (Tmux Plugins Manager)
